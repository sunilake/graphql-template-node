projects:
  app:
    schemaPath: "src/schema.graphql"
    extensions:
      endpoints:
        default: "http://localhost:4000"
  database:
    schemaPath: "src/database/schema.graphql"
    extensions:
      graphcool: graphcool.yml
